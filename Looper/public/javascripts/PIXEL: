Pixel:

I want to be able to sense physical space well enough, relative physical space on networked (via mesh) modules, so that a 3D structure can be "scanned" by building a point cloud of relative positioning (for object recognition and awareness). There could be a JavaScript interface to them.

Gesture Plotter Tasks:

- Store average curves for each label
- Compare curve of live data over some window, at some frequency over the timespan, with the average curve

++++

- provide blanks to fill in with functions that show the logical flow
- "flow"
- "chunking"
- "scaffolding"

++++